#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

echo "[fixtures] Running fixture suite"

# Replace with project-specific fixture execution command.
# Examples:
#   python3 tests/fixtures/run.py
#   npm run test:fixtures
[FIXTURE_TEST_COMMAND]

echo "[fixtures] Fixture suite passed"
